number = int(input('ведите проверяемое число - '))
listDivider = []
for i in range(1, number+1):
	if int(number/i) == number/i:
		listDivider.append(i)
if len(listDivider) > 2:
	print(f'{number} составное число\n {listDivider}\n делители числа {number}')
else:
	print(f'{number} простое число')
